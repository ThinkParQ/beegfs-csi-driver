
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: beegfsdrivers.beegfs.csi.netapp.com
spec:
  group: beegfs.csi.netapp.com
  names:
    kind: BeegfsDriver
    listKind: BeegfsDriverList
    plural: beegfsdrivers
    singular: beegfsdriver
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BeegfsDriver is the Schema for the beegfsdrivers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BeegfsDriverSpec defines the desired state of BeegfsDriver
            properties:
              containerImageOverrides:
                description: A structure that allows for default container images
                  and tags to be overridden. Use it in air-gapped networks, networks
                  with private registry mirrors, or to pin a particular container
                  version. Unless otherwise noted, versions other than the default
                  are not supported.
                properties:
                  beegfsCsiDriver:
                    description: Defaults to docker.io/netapp/beegfs-csi-driver:<the
                      operator version>.
                    properties:
                      image:
                        description: A combination of registry and image (e.g. k8s.gcr.io/csi-provisioner
                          or docker.io/netapp/beegfs-csi-driver).
                        type: string
                      tag:
                        description: A tag (e.g. v2.2.2 or latest).
                        type: string
                    type: object
                  csiNodeDriverRegistrar:
                    description: Defaults to k8s.gcr.io/sig-storage/csi-node-driver-registrar:<the
                      most current version at operator release>.
                    properties:
                      image:
                        description: A combination of registry and image (e.g. k8s.gcr.io/csi-provisioner
                          or docker.io/netapp/beegfs-csi-driver).
                        type: string
                      tag:
                        description: A tag (e.g. v2.2.2 or latest).
                        type: string
                    type: object
                  csiProvisioner:
                    description: Defaults to k8s.gcr.io/sig-storage/csi-provisioner:<the
                      most current version at operator release>.
                    properties:
                      image:
                        description: A combination of registry and image (e.g. k8s.gcr.io/csi-provisioner
                          or docker.io/netapp/beegfs-csi-driver).
                        type: string
                      tag:
                        description: A tag (e.g. v2.2.2 or latest).
                        type: string
                    type: object
                  livenessProbe:
                    description: Defaults to k8s.gcr.io/sig-storage/livenessprobe:<the
                      most current version at operator release>.
                    properties:
                      image:
                        description: A combination of registry and image (e.g. k8s.gcr.io/csi-provisioner
                          or docker.io/netapp/beegfs-csi-driver).
                        type: string
                      tag:
                        description: A tag (e.g. v2.2.2 or latest).
                        type: string
                    type: object
                type: object
              logLevel:
                description: The logging level of deployed containers expressed as
                  an integer from 0 (low detail) to 5 (high detail). Empty results
                  in the BeeGFS CSI driver project default.
                maximum: 5
                minimum: 0
                type: integer
              pluginConfig:
                description: The top level configuration structure containing default
                  configuration (applied to all file systems on all nodes), file system
                  specific configuration, and node specific configuration. Fields
                  from node and file system specific configurations override fields
                  from the default configuration. Often not required.
                properties:
                  config:
                    description: The primary configuration structure containing all
                      of the custom configuration (beegfs-client.conf keys/values
                      and additional CSI driver specific fields) associated with a
                      single BeeGFS file system except for sysMgmtdHost, which is
                      specified elsewhere.
                    properties:
                      beegfsClientConf:
                        additionalProperties:
                          type: string
                        description: A map of additional key value pairs matching
                          key value pairs in the beegfs-client.conf file. See beegfs-client.conf
                          for more details.
                        type: object
                      connInterfaces:
                        description: A list of interfaces the BeeGFS client service
                          can communicate over (e.g. "ib0" or "eth0"). Often not required.
                          See beegfs-client.conf for more details.
                        items:
                          type: string
                        type: array
                      connNetFilter:
                        description: A list of subnets the BeeGFS client service can
                          use for outgoing communication (e.g. "10.10.10.10/24").
                          Often not required. See beegfs-client.conf for more details.
                        items:
                          type: string
                        type: array
                      connTcpOnlyFilter:
                        description: A list of subnets in which RDMA communication
                          can/should not be established (e.g. "10.10.10.11/24"). Often
                          not required. See beegfs-client.conf for more details.
                        items:
                          type: string
                        type: array
                    type: object
                  fileSystemSpecificConfigs:
                    description: A list of file system specific configurations that
                      override the default configuration for specific file systems.
                    items:
                      description: A file system specific configuration that overrides
                        the default configuration for a specific file system.
                      properties:
                        config:
                          description: The primary configuration structure containing
                            all of the custom configuration (beegfs-client.conf keys/values
                            and additional CSI driver specific fields) associated
                            with a single BeeGFS file system except for sysMgmtdHost,
                            which is specified elsewhere.
                          properties:
                            beegfsClientConf:
                              additionalProperties:
                                type: string
                              description: A map of additional key value pairs matching
                                key value pairs in the beegfs-client.conf file. See
                                beegfs-client.conf for more details.
                              type: object
                            connInterfaces:
                              description: A list of interfaces the BeeGFS client
                                service can communicate over (e.g. "ib0" or "eth0").
                                Often not required. See beegfs-client.conf for more
                                details.
                              items:
                                type: string
                              type: array
                            connNetFilter:
                              description: A list of subnets the BeeGFS client service
                                can use for outgoing communication (e.g. "10.10.10.10/24").
                                Often not required. See beegfs-client.conf for more
                                details.
                              items:
                                type: string
                              type: array
                            connTcpOnlyFilter:
                              description: A list of subnets in which RDMA communication
                                can/should not be established (e.g. "10.10.10.11/24").
                                Often not required. See beegfs-client.conf for more
                                details.
                              items:
                                type: string
                              type: array
                          type: object
                        sysMgmtdHost:
                          description: The sysMgmtdHost used by the BeeGFS client
                            service to make initial contact with the BeeGFS mgmtd
                            service.
                          type: string
                      required:
                      - sysMgmtdHost
                      type: object
                    type: array
                  nodeSpecificConfigs:
                    description: A list of node specific configurations that override
                      file system specific configurations and the default configuration
                      on specific nodes.
                    items:
                      description: A node specific configuration that overrides file
                        system specific configurations and the default configuration
                        on specific nodes.
                      properties:
                        config:
                          description: The primary configuration structure containing
                            all of the custom configuration (beegfs-client.conf keys/values
                            and additional CSI driver specific fields) associated
                            with a single BeeGFS file system except for sysMgmtdHost,
                            which is specified elsewhere.
                          properties:
                            beegfsClientConf:
                              additionalProperties:
                                type: string
                              description: A map of additional key value pairs matching
                                key value pairs in the beegfs-client.conf file. See
                                beegfs-client.conf for more details.
                              type: object
                            connInterfaces:
                              description: A list of interfaces the BeeGFS client
                                service can communicate over (e.g. "ib0" or "eth0").
                                Often not required. See beegfs-client.conf for more
                                details.
                              items:
                                type: string
                              type: array
                            connNetFilter:
                              description: A list of subnets the BeeGFS client service
                                can use for outgoing communication (e.g. "10.10.10.10/24").
                                Often not required. See beegfs-client.conf for more
                                details.
                              items:
                                type: string
                              type: array
                            connTcpOnlyFilter:
                              description: A list of subnets in which RDMA communication
                                can/should not be established (e.g. "10.10.10.11/24").
                                Often not required. See beegfs-client.conf for more
                                details.
                              items:
                                type: string
                              type: array
                          type: object
                        fileSystemSpecificConfigs:
                          description: A list of file system specific configurations
                            that override the default configuration for specific file
                            systems on these nodes.
                          items:
                            description: A file system specific configuration that
                              overrides the default configuration for a specific file
                              system.
                            properties:
                              config:
                                description: The primary configuration structure containing
                                  all of the custom configuration (beegfs-client.conf
                                  keys/values and additional CSI driver specific fields)
                                  associated with a single BeeGFS file system except
                                  for sysMgmtdHost, which is specified elsewhere.
                                properties:
                                  beegfsClientConf:
                                    additionalProperties:
                                      type: string
                                    description: A map of additional key value pairs
                                      matching key value pairs in the beegfs-client.conf
                                      file. See beegfs-client.conf for more details.
                                    type: object
                                  connInterfaces:
                                    description: A list of interfaces the BeeGFS client
                                      service can communicate over (e.g. "ib0" or
                                      "eth0"). Often not required. See beegfs-client.conf
                                      for more details.
                                    items:
                                      type: string
                                    type: array
                                  connNetFilter:
                                    description: A list of subnets the BeeGFS client
                                      service can use for outgoing communication (e.g.
                                      "10.10.10.10/24"). Often not required. See beegfs-client.conf
                                      for more details.
                                    items:
                                      type: string
                                    type: array
                                  connTcpOnlyFilter:
                                    description: A list of subnets in which RDMA communication
                                      can/should not be established (e.g. "10.10.10.11/24").
                                      Often not required. See beegfs-client.conf for
                                      more details.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              sysMgmtdHost:
                                description: The sysMgmtdHost used by the BeeGFS client
                                  service to make initial contact with the BeeGFS
                                  mgmtd service.
                                type: string
                            required:
                            - sysMgmtdHost
                            type: object
                          type: array
                        nodeList:
                          description: The list of nodes this configuration should
                            be applied on. Each entry is the hostname of the node
                            or the name assigned to the node by the container orchestrator
                            (e.g. "node1" or "cluster05-node03").
                          items:
                            type: string
                          type: array
                      required:
                      - nodeList
                      type: object
                    type: array
                type: object
            type: object
          status:
            description: BeegfsDriverStatus defines the observed state of BeegfsDriver
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    type FooStatus struct{     // Represents the observations of a
                    foo's current state.     // Known .status.conditions.type are:
                    \"Available\", \"Progressing\", and \"Degraded\"     // +patchMergeKey=type
                    \    // +patchStrategy=merge     // +listType=map     // +listMapKey=type
                    \    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`
                    \n     // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
